# Fnord Network CA

[ default ]
ca                      = network-ca            # CA name
dir                     = .                     # Top dir
base_url                = http://fnord.no/ca    # CA base URL
aia_url                 = $base_url/$ca.cer     # Authority information access
crl_url                 = $base_url/$ca.crl     # CRL distribution point
cps_url                 = $base_url/            # CPS location
name_opt                = esc_ctrl,utf8,lname,sep_multiline,space_eq,align

# CA certificate request

[ req ]
default_bits            = 2048                  # RSA key size
encrypt_key             = yes                   # Protect private key
default_md              = sha1                  # MD to use
utf8                    = yes                   # Input is UTF-8
string_mask             = utf8only              # Emit UTF-8 strings
prompt                  = no                    # Don't prompt for DN
distinguished_name      = ca_dn                 # DN section
req_extensions          = ca_reqext             # Desired extensions

[ ca_dn ]
countryName             = "NO"
organizationName        = "Fnord AS"
organizationalUnitName  = "Fnord Certificate Authority"
commonName              = "Fnord Network CA"

[ ca_reqext ]
keyUsage                = critical,keyCertSign,cRLSign
basicConstraints        = critical,CA:true,pathlen:0
subjectKeyIdentifier    = hash

# CA operational settings

[ ca ]
default_ca              = network_ca            # The default CA section

[ network_ca ]
certificate             = $dir/ca/$ca.crt       # The CA cert
private_key             = $dir/ca/$ca/private/$ca.key # CA private key
new_certs_dir           = $dir/ca/$ca           # Certificate archive
serial                  = $dir/ca/$ca/db/$ca.crt.srl # Serial number file
crlnumber               = $dir/ca/$ca/db/$ca.crl.srl # CRL number file
database                = $dir/ca/$ca/db/$ca.db # Index file
unique_subject          = no                    # Require unique subject
default_days            = 730                   # How long to certify for
default_md              = sha1                  # MD to use
policy                  = match_pol             # Default naming policy
email_in_dn             = no                    # Add email to cert DN
preserve                = no                    # Keep passed DN ordering
name_opt                = $name_opt             # Subject DN display options
cert_opt                = ca_default            # Certificate display options
copy_extensions         = copy                  # Copy extensions from CSR
x509_extensions         = server_ext            # Default cert extensions
default_crl_days        = 1                     # How long before next CRL
crl_extensions          = crl_ext               # CRL extensions

[ match_pol ]
countryName             = match
stateOrProvinceName     = optional
localityName            = optional
organizationName        = match
organizationalUnitName  = optional
commonName              = optional              # Optional if SAN is present

[ client_pol ]
countryName             = supplied
stateOrProvinceName     = optional
localityName            = optional
organizationName        = optional
organizationalUnitName  = optional
commonName              = supplied
emailAddress            = supplied

[ any_pol ]
domainComponent         = optional
countryName             = optional
stateOrProvinceName     = optional
localityName            = optional
organizationName        = optional
organizationalUnitName  = optional
commonName              = optional
emailAddress            = optional

# Extensions

[ server_ext ]
keyUsage                = critical,digitalSignature,keyEncipherment
basicConstraints        = CA:false
extendedKeyUsage        = serverAuth,clientAuth
subjectKeyIdentifier    = hash
authorityKeyIdentifier  = keyid:always
authorityInfoAccess     = @authority_info_access
crlDistributionPoints   = @crl_distribution_points
certificatePolicies     = ia5org,@certificate_policy

[ client_ext ]
keyUsage                = critical,digitalSignature
basicConstraints        = CA:false
extendedKeyUsage        = clientAuth
subjectKeyIdentifier    = hash
authorityKeyIdentifier  = keyid:always
authorityInfoAccess     = @authority_info_access
crlDistributionPoints   = @crl_distribution_points
certificatePolicies     = ia5org,@certificate_policy

[ crl_ext ]
authorityKeyIdentifier  = keyid:always
authorityInfoAccess     = @authority_info_access

[ authority_info_access ]
caIssuers;URI.0         = $aia_url

[ crl_distribution_points ]
URI.0                   = $crl_url

[ certificate_policy ]
policyIdentifier        = anyPolicy
CPS.0                   = $cps_url
